/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["example.com", "https://rwa-finance.vercel.app"],
  },
};

module.exports = nextConfig;
